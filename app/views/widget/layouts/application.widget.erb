<!DOCTYPE html>
<html>
<head>
    <title>Palomino Labs - Beer Reviews Widget</title>
    <style type="text/css">
        body {
            font-family: 'Roboto', sans-serif;
            font-size: 16px;
            padding: 0;
            margin: 0;
            font-weight: 300;
            padding-bottom: 15px;
        }

        strong {
            font-weight: 500;
        }

        .list-table {
            border-spacing: 0;
            border-collapse: separate;
            cursor: default;
            width: 100%;
        }

        .list-table th {
            border-bottom: 1px solid #d9d9d9;
        }

        .list-table th, .list-table td {
            border-left: 1px solid #D9D9D9;
            padding: 5px;
            transition: background 0.25s;
            -webkit-transition: background 0.25s;
            -moz-transition: background 0.25s;
        }

        .list-table th:first-of-type, .list-table td:first-of-type {
            border-left-width: 0;
        }

        .activity-item {
            padding: 5px;
            cursor: default;
            border-bottom: 1px solid #D9D9D9;
            line-height: 20px;
            transition: background 0.25s;
            -webkit-transition: background 0.25s;
            -moz-transition: background 0.25s;
        }

        .list-table tbody tr:hover, .activity-item:hover {
            background: rgba(255, 204, 0, 0.1);
        }

        .activity-item img {
            height: 16px;
            vertical-align: baseline;
        }

        #shadow {
            position: fixed;
            bottom: 0;
            left: 0;
            right: 0;
            width: 100%;
            height: 30px;
            background: -moz-linear-gradient(top,  rgba(0,0,0,0) 0%, #fff 80%);
            background: -webkit-gradient(linear, left top, left bottom, color-stop(0%,rgba(0,0,0,0)), color-stop(80%,#fff));
            background: -webkit-linear-gradient(top,  rgba(0,0,0,0) 0%,#fff 80%);
            background: -o-linear-gradient(top,  rgba(0,0,0,0) 0%,#fff 80%);
            background: -ms-linear-gradient(top,  rgba(0,0,0,0) 0%,#fff 80%);
            background: linear-gradient(to bottom,  rgba(0,0,0,0) 0%,#fff 80%);
        }
    </style>
    <link href='http://fonts.googleapis.com/css?family=Roboto:300,400,500,700' rel='stylesheet' type='text/css'>
</head>
<body>

<%= yield %>

<div id="shadow"></div>

</body>
</html>
