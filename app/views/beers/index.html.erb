<div class="row">
    <div class="col-xs-12">
        <h1>Beers</h1>

        <%= render 'list' %>

        <br>
        <% if current_user %>
            <%= link_to 'New Beer', new_beer_path, class: 'btn btn-default' %>
        <% end %>
        <br>
    </div>
</div>

